=render @post
.ui.divider
%h3.ui.header Комментарии
= render partial: 'client/comments/comment', collection: @post.comments, cached: true
.ui.divider
- if user_signed_in?
  %h3.ui.header Написать
  = render 'client/comments/form'